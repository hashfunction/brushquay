# Reviewed optional resource omissions

Root reviewed the original run 34708230966 full staged PE graph: 537 PE files, no observation errors. Replaying that original graph through the existing `release_inputs.apply_exclusions` gate passes: none of the retained application or dependency PEs imports the optional OpenSSL 1.1 group, through normal or delay imports. The complete original runtime map has 5,106 files; excluding the exact three pins yields 5,103. This original result supports the decision; each fresh Windows run must independently satisfy the same current source/run/attempt/byte/origin/full-graph conditions.

The disposable stage now calls that existing gate before removing only its own copied `plugins/tls/qopensslbackend.dll`, `bin/libssl-1_1-x64.dll` and `bin/libcrypto-1_1-x64.dll`. Pins remain in `release_inputs.OPTIONAL_TLS`. The entire owned staged tree must match before removal, each removed file is rechecked, and the retained tree must match afterward. The native install and locked input trees are unchanged. The receipt retains `fullRuntimeInputs` and the original full graph; `runtimeInputs` describes the retained files, with explicit `excludedRuntime` and `optionalTlsRemovalReviewed: true`. An incomplete graph now refuses before SDK packing, retaining its original graph error. Schannel and Python's independent `libcrypto-3.dll` / `libssl-3.dll` are retained.

Reviewed release mode already applies the same graph/byte guard when `excludedRuntime` is the exact fixed group and `optionalTlsRemovalReviewed` is true. Binding preparation is separately owned; this change leaves `release-inputs.json` null and asserts no license/source completion.

The second omission is only `Krita_Artists_SeExpr_examples.bundle` from the production CMake install list. Its 34 optional examples declare unversioned CC-BY-SA in their original author metadata, with no verified version from the original author thread or history. The unchanged 2,211,603-byte source bundle (SHA256 `5e0a5a97fb31ab1b8840e3df03c64bb410d61166e5824d8b46d168bdc109a15c`) remains in source/history. SeExpr engine code and saved expression handling are unchanged. Generic resource discovery tolerates the absent examples; the preset widget checks an absent resource before using it and stored script text remains in configuration. Detailed grant proof is retained at `/private/tmp/bristlune-resource-grant-resolution-20260912`.

Local verification: four new production boundary cases pass, including actual CMake configure/install of the unchanged bundle rules. That installation contains exactly the other three original bundles plus README, each byte-identical to source. Tests also cover retained Schannel/Python libraries, only-three-file removal, incoming normal/delay edges, stale/incomplete/missing graph, changed/extra owned files and no removal on refusal. Ten preparation tests and five existing release-input tests pass. The original 537-PE graph replay passes; `git diff --check` passes. CMake installation here is a macOS fixture, not the fresh Windows app/package/consumer lifecycle required for release.
