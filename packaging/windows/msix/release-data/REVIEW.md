# Bristlune final release materials candidate

This candidate is ready for source/license review. It does not approve the
active release binding or claim a new installed Windows result.

## Exact selection and original inputs

The retained original package observation and complete 537-PE graph from
run 34708230966, attempt 1, bind source
`ef50a51eeacf4c9b857b99da34a0ddb60d0318ec`. `candidate-provenance.json`
records their original digests and native context. The original 5,106-file
selection projects to 5,102 files after only the approved three-file optional
OpenSSL1 group and source install of the SeExpr examples bundle are omitted.
The complete proposed map contains 870 application and 4,232 dependency
files, with 68 used owner decisions. It retains Schannel and Python OpenSSL3.

The original dependency catalog remains unchanged. The combined catalog
preserves all its entries and qualifications, adds 173 exact application
originals, four original W3C grant documents, and the source-owned Store terms:
1,470 files total. The source-owned original anonymous readback remains
150,494 bytes / SHA256
`3ba25ac4df6f885e4407a02a026a4fecc249df0f3260e70816397df9537f4de1`.
Its 119 verified public bodies cover 117 source archives and two metadata
assets. No source archive was downloaded or rehashed for this integration.

Application scope preserves the original GPL source, MIT Raqm/simplex terms,
CC0 default/RGBA resources, CC-BY/CC-BY-SA resource and ICC qualifications,
W3C Adobe PQ grant, and explicit original scRGB/krita25 profile grants.
Upstream workspaces and assets are not relabeled as Trieflow's own CC0 work.
The application aggregate expression is a review proposal. Dependency owner
expressions, component exceptions and custom LicenseRef meanings are retained
in the combined catalog and provenance, including separately licensed locale,
data, compiler and plugin scope.

The one permissive `ext_vpx` reference requires the exact original selected
libvpx DLL hash/origin, BSD-3-Clause expression and original 1.13.1
LICENSE/PATENTS pair. Its original `meson-2` producer revision remains unknown.
The source index explicitly separates this upstream version reference from
verified public archive bodies; it makes no exact source/reproduction claim.
There is no general source-reference exception for other owners or binaries.

## Store license text

`Bristlune-Store-license.txt` is the exact plain-text Partner Center candidate:
7,138 bytes / SHA256
`a2d9b8c7da10c8c4e2de69976f25c0607ea53a904b14368b0819e2323926b281`.
The packaged copy is identical. Its Microsoft-only protections cover the
three original SDK debug DLLs, four VC runtime DLL instances and Microsoft
Distributable Code within the Windows Python build, preserving all independent
open-source/resource rights. Original SDK, Python Additional Conditions, VC
and VS notices remain unchanged and are incorporated only in their applicable
scope. Runtime end-user terms alone are not claimed as a redistribution grant.

`store-license-basis.json` binds this text to the exact seven standalone
Microsoft DLL records and ten original Microsoft/Python notice files. The
candidate checker rejects changed Store text, packaged bytes, runtime scope,
original grant bytes or the original grant mapping.

## Verification and activation boundary

- `python3 -m unittest discover -s packaging/windows/msix/tests -v`: 55 tests
  pass, including exact libvpx/refusal cases, false-attestation candidate
  refusal by the actual audited builder, and Store text/runtime/original-grant
  mutation cases. New reference/candidate and Store validators were exercised
  red before their implementation, then green.
- `check_release_candidate.py` passes against the unchanged original package
  record and PE graph. It checks every notice byte, source-body reference,
  projected owner, original catalog prefix, omission and Store text binding.
  It reports both approval flags false and the original native run context.
- The implementation/data diff whitespace check passes with original notice
  files excluded. Original notices retain their existing trailing whitespace
  and final blank lines; their bytes must not be reformatted. `.gitattributes`
  preserves copied receipts and exact-byte release inputs on Windows.

The active `release-inputs.json` is still exactly the original 38 bytes,
SHA256 `505bafd25c0ba731653ef2748a2c92db0b1de32f897c080b4e7734b3fa440723`,
with `reviewed:null`. No workflow/build switch promotes the candidate.

After review of the proposed owner expressions, resource applicability and
Store terms, the reviewer can copy `review-candidate.json`'s `candidate`
object into the active binding's `reviewed` field and explicitly set the two
review attestations true. A fresh exact public-source native run must then
prove current selection/full PE graph, both installed lifecycles and the
independent unsigned export. This historical map and these local checks do
not substitute for those results. No native rebuild, push or publication was
performed by this integration.
