# SDK container filename repair

Run 34708230966 completed native compilation/tests, SDK packing and SDK unpacking. Independent ZIP verification then rejected `Bristlune/share/krita/paintoppresets/c%29_Pencil_1_Sketch_%28mypaint%29.myb`. Original `qualification-package.json` records the literal filesystem name, 5,711 bytes, SHA256 `67e84826b573159d2ff99446b721851b25318b12b01e067a284fdde4392c360c`; both SDK logs use that literal name. The tool is pinned SDK 10.0.26100.0, makeappx SHA256 `00fff202b71c1266b8c3899701e42b5468ea153b2b33a0a47215fe27695f16d0`.

Microsoft's original `Encoding::EncodeFileName` encodes parentheses, the listed punctuation and non-ASCII UTF-8 bytes. `AppxPackageWriter` applies it before adding ZIP parts; `AppxPackageObject` decodes when extracting. Primary source pinned for this review: [Encoding.cpp](https://github.com/microsoft/msix-packaging/blob/25a65f5c1690930813bcc10cdf1d59fa865f2bb1/src/msix/common/Encoding.cpp), [writer](https://github.com/microsoft/msix-packaging/blob/25a65f5c1690930813bcc10cdf1d59fa865f2bb1/src/msix/pack/AppxPackageWriter.cpp), [reader](https://github.com/microsoft/msix-packaging/blob/25a65f5c1690930813bcc10cdf1d59fa865f2bb1/src/msix/unpack/AppxPackageObject.cpp). Retained original source files and commit: `/private/tmp/bristlune-msix-uri-review`.

The repair only maps ZIP entry names to their audited filesystem names, once. Percent-containing names require strict UTF-8, uppercase escapes and exact re-encoding through the SDK's character table. Original path safety, Windows case/Unicode/component collision checks apply to decoded names, and original exact size/hash/manifest/full-tree checks still apply. Encoded metadata aliases, malformed escapes, unnecessary escapes, encoded separators, traversal, alternate spellings, incomplete trees and signing payloads are refused. Literal percent characters remain possible through `%25`, without recursive decoding. Existing direct literal entries remain supported.

Verification on macOS: the original source brush with Windows CRLF exactly recreates the recorded 5,711-byte SHA; its real ZIP regression first reproduced the precise rejection and now passes. Seven new container-boundary tests cover that record, punctuation/Unicode/percent names, malformed/noncanonical paths, hash/completeness, case/Unicode/encoded aliases and directory ownership; 27 existing package/identity/staging/SDK tests pass. `git diff --check` passes. These are local real ZIP tests, not a new Windows installed lifecycle or replay of the full failed MSIX. Original Windows metadata remains unchanged; fresh native qualification is required.
