# Bristlune assigned Store release integration

The release path is opt-in. The existing foundation qualification remains the default.
No source or license closure is inferred from installed success or archive counts.

1. Add fixed `qualification` and `store` contracts beside the existing manifest validator.
   Preserve BrushQuay Application.Id, assigned Store identity/publisher, data paths,
   Bristlune executable, version 1.0.1.0, x64/Desktop and existing capability scope.
2. Add a reviewed release-input interface, initially null. Bind original notice catalog,
   public source readback and exact runtime/source associations. Root supplies reviewed
   data separately; implementation never enables closure unconditionally.
3. Reuse current native receipt, source cleanliness, locked inputs, runtime selector and
   materializer. Observe the complete current PE graph before any reviewed optional TLS
   exclusion; reject incoming normal/delay references from retained files. Add unchanged
   original notices and source references, then build an exact audited runtime.
4. Reuse the audited MSIX builder for both fixed identities. Independently check mode at
   generated/container/unpacked/installed manifest boundaries. Bind source/tree/run/attempt,
   current helpers, native receipt, lock, SDK, source references and all payload hashes.
5. Extend only the installed lifecycle interface to accept a prepared fixed-mode package.
   Run separate Windows PowerShell 5.1 invocations with exact registration/certificate/PID/
   job/profile ownership. Preserve actual painting/save/PNG/reopen, binary pixel oracle,
   module acceptance, normal close, uninstall and original cleanup. Retain final evidence
   file hashes and explicit cleanup outcomes, including temporary signing state.
6. Independently export only the unsigned Store package after both original lifecycles.
   Reconstruct expected payload from current source/native inputs; require complete typed
   GUI, module, pixel oracle, original snapshots and cleanup records with exact run binding.
   Never reparse deleted fixture files or accept a staged/direct-launch substitute.
7. Wire explicit workflow_dispatch `release` default false. Preserve failure metadata;
   upload exactly one unsigned MSIX and one export receipt only after successful export.
8. Add focused positive and mutation fixtures: modes, source/public references, optional
   TLS reverse edges, payload/notices, stale/partial lifecycle/oracle, cleanup and signing
   leakage. Run targeted local suites; actual dual installed Windows remains pending.

Review boundaries: root owns source/catalog/audit data and product runtime repairs.
The final binding remains null until root reviews concrete source/license applicability.
Current OpenSSL applicability and full application import graph remain independent gates.
No native build, publication, Store or site changes are part of this implementation turn.
